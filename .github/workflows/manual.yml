name: Manual workflow test
#name of the workflow
on:
  workflow_dispatch:
    inputs:
     Buildenv:
       description: "Select the build environment"
       required: true
       type: string


jobs:
  Worflow_test:
    runs-on: ubuntu-latest
    steps:
      - name: Git workflow running
        uses: actions/checkout@v4

      - name: Workflow test
        run: echo "Github workflow is running successfully"
      
      - name: YML instructions working 
        run: echo "YML instructions are used to create workflow in github actions"
